import { UnicornAgePipe } from './unicorn-age.pipe';

describe('UnicornAgePipe', () => {
  it('create an instance', () => {
    const pipe = new UnicornAgePipe();
    expect(pipe).toBeTruthy();
  });
});
